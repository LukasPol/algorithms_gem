# frozen_string_literal: true

RSpec.describe AlgorithmsGem do
  it "has a version number" do
    expect(AlgorithmsGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
